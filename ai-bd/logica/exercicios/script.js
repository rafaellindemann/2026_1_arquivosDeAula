function banheiro(){
    alert("Ja fez ate o exercicio 6?")
}

function guilherme(){
    // ler estagiarios
    // ler clt
    // ler pj

    // calcular total
-   
    // mostrar resultado

}

function pePequeno(){
    let qtdPares = Number(prompt("Quantos pares?"))
    let precoPar = Number(prompt("Preço de cada par?"))

    let total = qtdPares * precoPar
    
    alert("Valor total R$" + total)
}

function peba(){   
   // ler o numero de vitorias
    let vitorias = Number(prompt("Quantas vitórias?"))
    // ler o numero de empates
    let empates = Number(prompt("Quantos empates?"))
    // calcular pontos
    let pontos = vitorias*3 + empates
    // mostrar o resultado
    alert("O time teu tem " + pontos)
}

