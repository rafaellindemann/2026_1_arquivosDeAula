const modulo = require('./nomeDoArquivo');

test("Texto para identificar o caso no terminal: ",() => {
    expect(chamaraFunção(valores) ).toBe(resultado);
})

test("", () =>  expect(
    somar(1,1)
)).toBe(2)